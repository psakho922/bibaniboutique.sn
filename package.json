{
  "name": "vinted-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "typecheck": "npm --workspace=@vinted/api run typecheck",
    "dev:api": "npm --workspace=@vinted/api run dev",
    "lint": "npm --workspace=@vinted/api run lint"
  }
}
